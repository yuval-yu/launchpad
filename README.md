# launchpad

StoryFun 发射台（自研合约 · Robinhood Chain）后端设计文档。

- `docs/index.html` — 发射台自建索引层设计（Envio 自建 + Java + MySQL），单文件 HTML，左侧标签分页。
  本地直接用浏览器打开即可；也可在仓库 Settings → Pages 选 `main` / `docs` 发布成站点。

设计文档同时维护在 claude.ai artifact，以此仓库中的文件为准。
